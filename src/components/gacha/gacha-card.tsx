import { Card, Typography } from "antd";

export default function GachaCard(data: GachaLog) {
    // const { Title, Paragraph } = Typography;

    return (
        <Card className="gacha-card">
            {/* <Title level={3}>{item.name} ({item.rarity}*)</Title>
            <Paragraph>ID: {item.id}</Paragraph>
            <Paragraph>Date: {item.date}</Paragraph> */}
        </Card>
    );
};